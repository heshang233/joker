ext {
    remotes {
        developServer {
            host = "10.0.251.150"
            user = "root"
            password = "123456"
            knownHosts = allowAnyHosts
        }
    }

    // 第三方依赖版本号管理
    springFrameworkVersion = '4.3.9.RELEASE'
    fasterxmlJacksonVersion = '2.9.4'
}